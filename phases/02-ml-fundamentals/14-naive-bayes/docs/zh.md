# 朴素贝叶斯（Naive Bayes）

> "朴素"（Naive）的假设是错误的，但它仍然有效。这就是它的美妙之处。

**类型：** 构建（Build）
**语言：** Python
**前置要求：** 第 2 阶段，第 01-07 课（分类、贝叶斯定理）
**时间：** 约 75 分钟

## 学习目标（Learning Objectives）

- 从零实现带有拉普拉斯平滑（Laplace Smoothing）的多项式朴素贝叶斯（Multinomial Naive Bayes），用于文本分类
- 解释为什么朴素独立性假设在数学上是错误的，但在实践中能产生正确的类别排序
- 比较多形式（Multinomial）、伯努利（Bernoulli）和高斯（Gaussian）朴素贝叶斯变体，并为给定的特征类型选择合适的变体
- 在高维稀疏数据上评估朴素贝叶斯与逻辑回归（Logistic Regression）的对比，并解释其中的偏差-方差权衡（Bias-Variance Tradeoff）

## 问题（The Problem）

你需要对文本进行分类。将邮件分为垃圾邮件或非垃圾邮件。将客户评论分为正面或负面。将工单分为不同类别。你有成千上万个特征（每个词一个特征），但训练数据有限。

大多数分类器在这里都会出问题。逻辑回归需要足够的样本来可靠地估计成千上万个权重。决策树（Decision Tree）一次只在一个词上分裂，会严重过拟合。KNN 在 10,000 维空间中毫无意义，因为每个点与其他每个点的距离都差不多。

朴素贝叶斯能处理这种情况。它做了一个数学上错误的假设（给定类别后，每个特征与其他所有特征独立），但在文本分类上仍然优于"更聪明"的模型，尤其是在训练集较小的情况下。它只需一次遍历数据即可完成训练。它可以扩展到数百万个特征。它能产生概率估计（尽管由于独立性假设，概率通常校准不佳）。

理解为什么一个错误的假设能产生好的预测，能让你学到机器学习中一个根本性的道理：最好的模型不是最正确的模型，而是对你的数据具有最佳偏差-方差权衡的模型。

## 概念（The Concept）

### 贝叶斯定理（Bayes' Theorem）快速回顾

贝叶斯定理翻转条件概率：

```
P(类别 | 特征) = P(特征 | 类别) * P(类别) / P(特征)
```

我们想要 `P(类别 | 特征)`——给定文档中的词，该文档属于某个类别的概率。我们可以通过以下方式计算：
- `P(特征 | 类别)`——在该类别的文档中看到这些词的似然（Likelihood）
- `P(类别)`——该类别的先验概率（Prior Probability）（垃圾邮件总体上有多常见？）
- `P(特征)`——证据（Evidence），对所有类别相同，因此在比较时可以忽略

具有最高 `P(类别 | 特征)` 的类别胜出。

### 朴素独立性假设（Naive Independence Assumption）

精确计算 `P(特征 | 类别)` 需要估计所有特征的联合概率。对于一个包含 10,000 个词的词汇表，你需要估计 2^10,000 种可能组合上的分布。这是不可能的。

朴素假设：给定类别后，每个特征条件独立。

```
P(w1, w2, ..., wn | 类别) = P(w1 | 类别) * P(w2 | 类别) * ... * P(wn | 类别)
```

与其估计一个不可能的联合分布，你只需估计 n 个简单的逐特征分布。每个分布只需要一个计数。

这个假设显然是错误的。在任何文档中，"机器"和"学习"这两个词都不是独立的。但分类器不需要正确的概率估计。它需要正确的排序——哪个类别具有最高的概率。独立性假设引入了系统性误差，但这些误差对所有类别的影响相似，因此排序保持正确。

### 为什么它仍然有效

三个原因：

1. **排序优于校准。** 分类只需要排名最高的类别是正确的。即使 P(垃圾邮件) = 0.99999 而真实概率是 0.7，分类器仍然正确地选择了垃圾邮件。我们不需要正确的概率。我们需要正确的赢家。

2. **高偏差，低方差。** 独立性假设是一个强先验。它严重约束了模型，从而防止过拟合。在训练数据有限的情况下，一个稍微错误但稳定的模型优于一个理论上正确但极不稳定的模型。这就是偏差-方差权衡的实际体现。

3. **特征冗余相互抵消。** 相关特征提供冗余证据。分类器会重复计算这些证据，但它也会为正确的类别重复计算。如果"机器"和"学习"总是一起出现，两者都为"技术"类别提供证据。朴素贝叶斯将它们计算两次，但它为正确的类别计算两次。

第四个实用原因：朴素贝叶斯非常快。训练只需一次遍历数据，统计频率。预测是一次矩阵乘法。你可以在几秒钟内训练一百万个文档。这种速度意味着你可以比使用更慢的模型更快地迭代、尝试更多特征集、运行更多实验。

### 逐步数学推导

让我们通过一个具体例子来追踪。假设我们有两个类别：垃圾邮件和非垃圾邮件。我们的词汇表有三个词："免费"、"钱"、"会议"。

训练数据：
- 垃圾邮件中提到"免费"80 次，"钱"60 次，"会议"10 次（共 150 个词）
- 非垃圾邮件中提到"免费"5 次，"钱"10 次，"会议"100 次（共 115 个词）
- 40% 的邮件是垃圾邮件，60% 是非垃圾邮件

使用拉普拉斯平滑（alpha=1）：

```
P(免费 | 垃圾邮件)    = (80 + 1) / (150 + 3) = 81/153 = 0.529
P(钱 | 垃圾邮件)      = (60 + 1) / (150 + 3) = 61/153 = 0.399
P(会议 | 垃圾邮件)    = (10 + 1) / (150 + 3) = 11/153 = 0.072

P(免费 | 非垃圾邮件)  = (5 + 1) / (115 + 3) = 6/118 = 0.051
P(钱 | 非垃圾邮件)    = (10 + 1) / (115 + 3) = 11/118 = 0.093
P(会议 | 非垃圾邮件)  = (100 + 1) / (115 + 3) = 101/118 = 0.856
```

新邮件包含："免费"（2 次），"钱"（1 次），"会议"（0 次）。

```
log P(垃圾邮件 | 邮件) = log(0.4) + 2*log(0.529) + 1*log(0.399) + 0*log(0.072)
                       = -0.916 + 2*(-0.637) + (-0.919) + 0
                       = -3.109

log P(非垃圾邮件 | 邮件) = log(0.6) + 2*log(0.051) + 1*log(0.093) + 0*log(0.856)
                         = -0.511 + 2*(-2.976) + (-2.375) + 0
                         = -8.838
```

垃圾邮件以大幅优势胜出。"免费"一词出现两次是垃圾邮件的强有力证据。注意，"会议"未出现对两个对数和的贡献为零（0 * log(P)）——在多项式中，缺失的词没有影响。伯努利朴素贝叶斯才会显式地建模词的缺失。

### 三种变体

朴素贝叶斯有三种形式。每种以不同的方式建模 `P(特征 | 类别)`。

#### 多项式朴素贝叶斯（Multinomial Naive Bayes）

将每个特征建模为计数。最适合文本数据，其中特征是词频或 TF-IDF 值。

```
P(词_i | 类别) = (类别中词_i的计数 + alpha) / (类别中总词数 + alpha * 词汇表大小)
```

`alpha` 是拉普拉斯平滑（下文解释）。这种变体是文本分类的主力。

#### 高斯朴素贝叶斯（Gaussian Naive Bayes）

将每个特征建模为正态分布。最适合连续特征。

```
P(x_i | 类别) = (1 / sqrt(2 * pi * 方差)) * exp(-(x_i - 均值)^2 / (2 * 方差))
```

每个类别在每个特征上有自己的均值和方差。当特征在每个类别内确实遵循钟形曲线时，这种方法效果很好。

#### 伯努利朴素贝叶斯（Bernoulli Naive Bayes）

将每个特征建模为二值（出现或不出现）。最适合短文本或二值特征向量。

```
P(词_i | 类别) = (类别中包含词_i的文档数 + alpha) / (类别中文档总数 + 2 * alpha)
```

与多项式不同，伯努利显式地惩罚词的缺失。如果"免费"通常出现在垃圾邮件中但在这封邮件中缺失，伯努利将其计为反对垃圾邮件的证据。

### 何时使用每种变体

| 变体 | 特征类型 | 最适合 | 示例 |
|------|---------|--------|------|
| 多项式（Multinomial） | 计数或频率 | 文本分类，词袋模型（Bag-of-Words） | 邮件垃圾过滤，主题分类 |
| 高斯（Gaussian） | 连续值 | 具有近似正态分布特征的表格数据 | 鸢尾花分类，传感器数据 |
| 伯努利（Bernoulli） | 二值（0/1） | 短文本，二值特征向量 | 短信垃圾过滤，存在/缺失特征 |

### 拉普拉斯平滑（Laplace Smoothing）

当一个词出现在测试数据中但从未出现在某个类别的训练数据中时会发生什么？

没有平滑：`P(词 | 类别) = 0/N = 0`。一个零乘以整个乘积会使 `P(类别 | 特征) = 0`，无论其他所有证据如何。一个未见过的词会摧毁整个预测，无论有多少其他证据支持它。

拉普拉斯平滑为每个特征计数添加一个小计数 `alpha`（通常为 1）：

```
P(词_i | 类别) = (count(词_i, 类别) + alpha) / (类别中总词数 + alpha * 词汇表大小)
```

当 alpha=1 时，每个词至少获得一个微小的概率。测试邮件中出现"discombobulate"这个词不再会扼杀垃圾邮件的概率。平滑有一个贝叶斯解释：它等价于在词分布上放置一个均匀的狄利克雷先验（Dirichlet Prior）。

更高的 alpha 意味着更强的平滑（更均匀的分布）。更低的 alpha 意味着模型更信任数据。alpha 是一个需要调优的超参数。

alpha 的效果：

| Alpha | 效果 | 何时使用 |
|-------|------|---------|
| 0.001 | 几乎没有平滑，信任数据 | 非常大的训练集，预计不会出现未见过的特征 |
| 0.1 | 轻度平滑 | 大型训练集 |
| 1.0 | 标准拉普拉斯平滑 | 默认起点 |
| 10.0 | 重度平滑，使分布趋于均匀 | 非常小的训练集，预计会出现许多未见过的特征 |

### 对数空间计算（Log-Space Computation）

将数百个概率（每个都小于 1）相乘会导致浮点下溢（Floating-Point Underflow）。乘积在浮点数中变为零，尽管真实值是一个非常小的正数。

解决方案：在对数空间中工作。与其将概率相乘，不如将它们的对数相加：

```
log P(类别 | x1, x2, ..., xn) = log P(类别) + sum_i log P(xi | 类别)
```

这将预测转化为点积：

```
log_scores = X @ log_feature_probs.T + log_class_priors
prediction = argmax(log_scores)
```

矩阵乘法。这就是为什么朴素贝叶斯预测如此之快——它与单层线性模型是相同的操作。

### 朴素贝叶斯 vs 逻辑回归（Logistic Regression）

两者都是用于文本的线性分类器。区别在于它们建模的内容。

| 方面 | 朴素贝叶斯 | 逻辑回归 |
|------|-----------|---------|
| 类型 | 生成式（Generative，建模 P(X\|Y)） | 判别式（Discriminative，建模 P(Y\|X)） |
| 训练 | 统计频率 | 优化损失函数 |
| 小数据 | 更好（强先验有帮助） | 更差（不足以估计权重） |
| 大数据 | 更差（错误假设有害） | 更好（灵活的决策边界） |
| 特征 | 假设独立 | 处理相关性 |
| 速度 | 单次遍历，非常快 | 迭代优化 |
| 校准 | 概率校准差 | 概率校准更好 |

经验法则：从朴素贝叶斯开始。如果你有足够的数据且朴素贝叶斯达到瓶颈，切换到逻辑回归。

### 分类流水线

```mermaid
flowchart LR
    A[原始文本（Raw Text）] --> B[分词（Tokenize）]
    B --> C[构建词汇表（Build Vocabulary）]
    C --> D[统计词频（Count Word Frequencies）]
    D --> E[应用平滑（Apply Smoothing）]
    E --> F[计算对数概率（Compute Log Probabilities）]
    F --> G[预测：argmax P类别给定词]

    style A fill:#f9f,stroke:#333
    style G fill:#9f9,stroke:#333
```

在实践中，我们在对数空间中工作以避免浮点下溢。与其将许多小概率相乘，不如将它们的对数相加：

```
log P(类别 | 特征) = log P(类别) + sum_i log P(特征_i | 类别)
```

## 构建它（Build It）

`code/naive_bayes.py` 中的代码从零实现了 MultinomialNB 和 GaussianNB。

### MultinomialNB

从零实现：

1. **fit(X, y)**：对于每个类别，统计每个特征的频率。添加拉普拉斯平滑。计算对数概率。存储类别先验（类别频率的对数）。

2. **predict_log_proba(X)**：对于每个样本，计算所有类别的 log P(类别) + sum of log P(特征_i | 类别)。这是一个矩阵乘法：X @ log_probs.T + log_priors。

3. **predict(X)**：返回具有最高对数概率的类别。

```python
class MultinomialNB:
    def __init__(self, alpha=1.0):
        self.alpha = alpha

    def fit(self, X, y):
        classes = np.unique(y)
        n_classes = len(classes)
        n_features = X.shape[1]

        self.classes_ = classes
        self.class_log_prior_ = np.zeros(n_classes)
        self.feature_log_prob_ = np.zeros((n_classes, n_features))

        for i, c in enumerate(classes):
            X_c = X[y == c]
            self.class_log_prior_[i] = np.log(X_c.shape[0] / X.shape[0])
            counts = X_c.sum(axis=0) + self.alpha
            self.feature_log_prob_[i] = np.log(counts / counts.sum())

        return self
```

关键洞察：拟合后，预测只是矩阵乘法加偏置。这就是朴素贝叶斯如此之快的原因。

### GaussianNB

对于连续特征，我们按类别按特征估计均值和方差：

```python
class GaussianNB:
    def __init__(self):
        pass

    def fit(self, X, y):
        classes = np.unique(y)
        self.classes_ = classes
        self.means_ = np.zeros((len(classes), X.shape[1]))
        self.vars_ = np.zeros((len(classes), X.shape[1]))
        self.priors_ = np.zeros(len(classes))

        for i, c in enumerate(classes):
            X_c = X[y == c]
            self.means_[i] = X_c.mean(axis=0)
            self.vars_[i] = X_c.var(axis=0) + 1e-9
            self.priors_[i] = X_c.shape[0] / X.shape[0]

        return self
```

预测使用每个特征的高斯概率密度函数（PDF），跨特征相乘（在对数空间中相加）。

### 演示：文本分类

代码生成模拟两个类别（技术文章 vs 体育文章）的合成词袋数据。每个类别有不同的词频分布。MultinomialNB 使用词计数对它们进行分类。

合成数据的工作原理如下：我们创建 200 个"词"（特征列）。词 0-39 在技术文章中频率高，在体育文章中频率低。词 80-119 在体育文章中频率高，在技术文章中频率低。词 40-79 在两者中频率中等。这创建了一个现实的场景，其中一些词是强类别指示器，而其他词是噪声。

## 你构建了什么（What You Built）

你从零实现了两个朴素贝叶斯分类器。你的实现：

1. **MultinomialNB** 使用词计数和拉普拉斯平滑进行文本分类，在对数空间中计算以避免下溢
2. **GaussianNB** 使用按类别的高斯分布处理连续特征
3. 两者都遵循相同的接口：`fit(X, y)` 学习参数，`predict(X)` 返回类别标签

关键要点：朴素贝叶斯之所以有效，不是因为它正确的——它的核心假设是错误的——而是因为它的偏差-方差权衡非常适合高维稀疏数据。强独立性先验防止过拟合，而对数空间计算使其极快。

## 反思问题（Reflection Questions）

1. 如果两个特征完全相关（例如，x2 = 2 * x1），朴素贝叶斯会如何处理？这对分类决策有什么影响？
2. 拉普拉斯平滑的 alpha 参数如何影响偏差-方差权衡？非常小的 alpha（如 0.001）和非常大的 alpha（如 100）各有什么风险？
3. 在什么情况下你会选择伯努利朴素贝叶斯而不是多项式朴素贝叶斯？考虑一个情感分析的例子，其中文档长度差异很大。
